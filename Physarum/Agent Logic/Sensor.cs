namespace Physarum.AgentLogic
{
    internal class Sensor
    {
        public float angle;
        public float length;
        public Sensor(float angle = 0, float length = 0)
        {
            this.angle = angle;
            this.length = length;
        }
    }
}
